1. Add an existing codebase to a new git repository

	git init
	git add .
	git commit -m "Commit initial sources"
	git remote add origin https://github.com/githubblackhorse/corefundamentals.git
	git push origin master
	git stat

2. Add changed files to the existing git repository

	git add .
	git commit -m "Commit modified source"
	
	